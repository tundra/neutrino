# Copyright 2013 the Neutrino authors (see AUTHORS).
# Licensed under the Apache License, Version 2.0 (see LICENSE).

## The built-in type of runtime-native null singleton.
def @Null := @ctrino.get_builtin_type("Null");

def type @Null is @Object;

## Returns true iff the right argument is null.
## TODO: should be a special case of object equality.
@builtin("null==*")
def ($this is @Null)==($that);
