# Copyright 2015 the Neutrino authors (see AUTHORS).
# Licensed under the Apache License, Version 2.0 (see LICENSE).

import $assert;
import $core;

def $test_simple() {
  def $pipe := new @core:io:Pipe();
  $assert:equals("Hello, World!", $pipe.out.write("Hello, World!"));
}

do {
  $test_simple();
}
