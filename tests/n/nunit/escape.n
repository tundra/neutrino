# Copyright 2013 the Neutrino authors (see AUTHORS).
# Licensed under the Apache License, Version 2.0 (see LICENSE).

import $assert;

def $test_simple_escape() {
  def $x := with_escape $leave do $leave(0);
  $assert:equals(0, $x);
}

# A method that does something, anything.
def $main() {
  $test_simple_escape();
}

entry_point $main();
